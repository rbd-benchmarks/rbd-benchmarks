## RobCoGen

Subdirectories:

* `eigen` contains the Eigen matrix library (version 3.3.5)
* `kindsl` contains `.kindsl` files that correspond to the `.urdf` files in `../urdf/`
* `gen` contains the C++ files generated by RobCoGen
* `iit` contains the `iit::rbd` header files
